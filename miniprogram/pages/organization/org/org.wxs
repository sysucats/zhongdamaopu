var isSelected = function(selected, index) {
  if (selected == undefined || index == undefined) {
    return false;
  }
  return selected.indexOf(index) != -1;
}

module.exports = {
  isSelected: isSelected
};